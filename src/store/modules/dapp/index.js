import mutations from './mutations';
// import actions from './actions';
// import getters from './getters';

export const state = {
  loaded: false,
  loading: false,
  requests: {},
  list: [],
};

export default {
  state,
  mutations,
  // actions,
  // getters,
};
